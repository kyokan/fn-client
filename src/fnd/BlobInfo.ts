export interface BlobInfo {
  name: string
  publicKey: string
  importHeight: number
  timestamp: number
  merkleRoot: string
  reservedRoot: string
  receivedAt: number
  signature: string
  timebank: number
}
